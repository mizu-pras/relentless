---
description: "Display pursuit analytics and performance metrics."
---

Arguments: $ARGUMENTS

Load the `relentless:metrics` skill and follow its instructions.

Pass arguments to the skill:
- No arguments: show summary metrics
- `--detailed`: show detailed breakdown
- `--json`: output raw JSON metrics
